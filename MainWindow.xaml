<Window x:Class="CyberSecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CyberSecurityChatbot"
        Title="Cybersecurity Awareness System"
        Height="620" Width="920"
        MinHeight="520" MinWidth="700"
        Background="#0D0F1A"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Shared palette -->
        <SolidColorBrush x:Key="BgDeep"      Color="#0D0F1A"/>
        <SolidColorBrush x:Key="BgPanel"     Color="#111827"/>
        <SolidColorBrush x:Key="AccentCyan"  Color="#00FFFF"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#00FF88"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextMuted"   Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderCol"   Color="#1E3A5F"/>

        <!-- Tab item style -->
        <Style x:Key="CyberTab" TargetType="TabItem">
            <Setter Property="Background"   Value="#111827"/>
            <Setter Property="Foreground"   Value="#94A3B8"/>
            <Setter Property="BorderBrush"  Value="#1E3A5F"/>
            <Setter Property="Padding"      Value="18,10"/>
            <Setter Property="FontSize"     Value="12"/>
            <Setter Property="FontWeight"   Value="SemiBold"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderCol}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="6,6,0,0"
                                Margin="2,4,2,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="TabContent"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#0D0F1A"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#00FFFF"/>
                                <Setter Property="Foreground" Value="#00FFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#1A2740"/>
                                <Setter Property="Foreground" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl style -->
        <Style TargetType="TabControl">
            <Setter Property="Background"       Value="#0D0F1A"/>
            <Setter Property="BorderBrush"      Value="#1E3A5F"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Padding"          Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BgDeep}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══ TOP HEADER BAR ═══ -->
        <Border Grid.Row="0" Background="{StaticResource BgPanel}"
                BorderBrush="#1E3A5F" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="36" Height="36"
                        Background="#001F3F" CornerRadius="8" VerticalAlignment="Center">
                    <TextBlock Text="🛡" FontSize="18"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                    <TextBlock Text="Y.O.U  CYBERSECURITY AWARENESS SYSTEM"
                               Foreground="{StaticResource AccentCyan}"
                               FontSize="13" FontWeight="Bold"
                               FontFamily="Consolas"/>
                    <TextBlock Text="Password • Phishing • Privacy • Tasks • Quiz"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontFamily="Consolas"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentGreen}"
                             VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="ONLINE" Foreground="{StaticResource AccentGreen}"
                               FontSize="10" FontWeight="Bold"
                               FontFamily="Consolas" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══ TAB CONTROL ═══ -->
        <TabControl Grid.Row="1" x:Name="MainTabs">

            <!-- ── Chat Tab ── -->
            <TabItem Style="{StaticResource CyberTab}" Header="💬  Chat">
                <local:ChatbotGUI x:Name="ChatTab"/>
            </TabItem>

            <!-- ── Tasks Tab ── -->
            <TabItem Style="{StaticResource CyberTab}" Header="📋  Tasks">
                <local:TaskManagerControl x:Name="TaskTab"/>
            </TabItem>

            <!-- ── Quiz Tab ── -->
            <TabItem Style="{StaticResource CyberTab}" Header="🎮  Quiz">
                <local:QuizControl x:Name="QuizTab"/>
            </TabItem>

            <!-- ── Activity Log Tab ── -->
            <TabItem Style="{StaticResource CyberTab}" Header="📜  Activity Log">
                <local:ActivityLogControl x:Name="LogTab"/>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
